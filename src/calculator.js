window.calculator = {
    //...
    suma: (a, b) =>  Number(a) + Number(b), //suma es una propiedad Ya pasa el test de que e suna función
    resta : (a, b) => Number(a) - Number(b),
    multiplicacion : (a, b) => Number(a) * Number(b),
    division : (a, b) => Number(a) / Number(b),
}